{% if settings.ml_recently_viewed %}
  {{ 'component-cart-drawer.css' | asset_url | stylesheet_tag }}
  <style>
    .drawer {
      visibility: hidden;
    }
  </style>
  <ml-recently-viewed-drawer class="drawer ml-drawer">
    <div class="ml-recently-viewed-drawer">
      <div class="drawer__overlay"></div>
      <ml-recently-viewed-container
        class="ml-recently-viewed-container"
        data-img-ratio="ratio-1x1"
      >
        <div
          class="drawer__inner gradient color-{{ settings.cart_color_scheme }}"
          role="dialog"
          aria-modal="true"
          aria-label="{{ 'custom.recently_viewed.title' | t }}"
          tabindex="-1"
        >
          <div class="drawer__inner-empty ml-recently-viewed-empty" hidden>
            <div class="drawer__empty-content">
              <h2 class="title">
                {{ 'custom.recently_viewed.empty' | t }}
              </h2>
              <button
                class="drawer__close"
                type="button"
                onclick="this.closest('ml-recently-viewed-drawer').close()"
                aria-label="{{ 'accessibility.close' | t }}"
              >
                <span class="svg-wrapper">
                  {{- 'icon-close.svg' | inline_asset_content -}}
                </span>
              </button>
              <a href="{{ routes.all_products_collection_url }}" class="button">
                {{ 'general.continue_shopping' | t }}
              </a>
            </div>
          </div>
          <div class="ml-recently-viewed-drawer-product-list-wrapper" hidden>
            <div class="drawer__header">
              <h2 class="drawer__heading">{{ 'custom.recently_viewed.title' | t }}</h2>
              <button
                class="drawer__close"
                type="button"
                onclick="this.closest('ml-recently-viewed-drawer').close()"
                aria-label="{{ 'accessibility.close' | t }}"
              >
                <span class="svg-wrapper">
                  {{- 'icon-close.svg' | inline_asset_content -}}
                </span>
              </button>
            </div>
            <div class="ml-recently-viewed-drawer-product-list ml-grid-product-list" role="list">
              {% comment %} Injected dynamically {% endcomment %}
            </div>
            <button
              class="ml-recently-viewed-btm-remove-all button button--primary button--full-width"
              type="button"
              onclick="this.closest('ml-recently-viewed-drawer').close()"
            >
              {{ 'custom.recently_viewed.remove_all' | t }}
            </button>
          </div>
        </div>
      </ml-recently-viewed-container>
    </div>
  </ml-recently-viewed-drawer>
{% endif %}
